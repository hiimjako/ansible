- include_vars: "Ubuntu.yml"
  tags: always
  when: ansible_distribution == "Ubuntu"

- name: Include ubuntu tasks
  include_tasks: "{{ item }}"
  loop:
    - apt.yml
    - brave.yml
    - lsd.yml
  tags: always
  when: ansible_distribution == "Ubuntu"

