---
- include_vars: "{{ ansible_distribution }}.yml"
  tags: always

- include: common/main.yml
    
- include: arch/main.yml
  when: ansible_distribution == "Archlinux"

- include: ubuntu/main.yml
  when: ansible_distribution == "Ubuntu"

